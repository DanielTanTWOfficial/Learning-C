/*  PROBLEM: Write a program that takes in user input strings separated by (1) spaces, (2) dashes(-), (3) commas(,),
    splits the strings into an array of individual words, and prints these words onto the console.
    E.g.
    Input string separated by spaces: hello world
    hello
    world
    Input string separated by dashes(-): hello-world
    hello
    world
    Input string separated by commas(,): hello,world
    hello
    world
*/
